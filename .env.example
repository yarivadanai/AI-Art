# Database connection (Neon PostgreSQL recommended for Vercel deployment)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/hit_arc?schema=public"

# Optional: absolute origin for shared-link Open Graph images (custom domain). On Vercel the deployment URL is used when unset.
# NEXT_PUBLIC_SITE_URL="https://mica.example.com"
